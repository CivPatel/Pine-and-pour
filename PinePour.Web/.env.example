# Web (Vite)
#
# In local dev, `VITE_API_BASE_URL` can be blank to use the Vite proxy.
# In production (Vercel), set it to your Render API base URL (no trailing slash).
VITE_API_BASE_URL=https://pine-and-pour.onrender.com

